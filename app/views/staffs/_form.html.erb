<%= form_for(@staff) do |f| %>
  <% if @staff.errors.any? %>
    <p><%= pluralize(@staff.errors.count, "Errors found should be fixed") %>
   
    <ul>
    <% @staff.errors.full_messages.each do |m| %>
      <li><%= m %></li>
    <% end %>
    </ul>
  <% end %>
    
<% end %>
